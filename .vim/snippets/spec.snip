# vim:set noet :
# スニペットのインデントはタブ文字で入力する必要あり
# :set noet で expandtab設定を一時的に変更できる

snippet	des
abbr	describe do
	describe '${1:DESCRIPTION}' do
		${2:TARGET}
	end

snippet	con do
abbr	context do
	context '${1:DESCRIPTION}' do
		${2:TARGET}
	end

snippet	bef
abbr	before do
	before do
		${1:TARGET}
	end

snippet	it
abbr	it do
	it '${1:DESCRIPTION}' do
		${2:TARGET}
	end

snippet	file
abbr	file read
	open("${1:FILE_NAME}","${2:OPEN_MODE}") do |file|
		file.each do |line|
			${3:TARGET}
		end
	end

