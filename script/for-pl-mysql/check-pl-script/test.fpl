#!/usr/bin/perl

use warnings;
use strict;
use utf8;

use 5.8.9;
use CGI::Fast qw(:standard);

my $COUNTER = 0;

while (new CGI::Fast) {
  print header;
  print start_html("Fast CGI Rocks");
  print
    h1("Fast CGI Rocks"), 
    "Invocation number ", b($COUNTER++), 
    hr;
  print end_html;
}
